import React from "react";
import Sidebar from '../Components/Sidebar';

const Products = () => {
  return (
    <React.Fragment>
      <Sidebar />
    </React.Fragment>
  );
};

export default Products;
